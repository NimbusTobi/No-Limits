import * as React from 'react'
import Layout from '../components/layout'

const IndexPage = () => {
	return (
		<Layout pageTitle="No Limits">
			<p>Welcome to pushing your limits</p>
		</Layout>
	)
}

export default IndexPage